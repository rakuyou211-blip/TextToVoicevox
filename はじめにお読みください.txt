テキスト抽出 → VOICEVOX の始め方

■ Windows の人
  「起動.bat」をダブルクリック。これだけです。
  （初回は自動でセットアップが走ります。初回だけネット接続が必要・数分かかります）

  「WindowsによってPCが保護されました」と出たら:
    小さな「詳細情報」をクリック → 「実行」ボタン。通すのはこの1回だけです。
    （初回に 起動.bat が、フォルダ内の警告ブロックを自動で外します。
      作者が有料のコード署名をしていないだけで、中身は全部公開しています）
  それでも動かないとき（保険）:
    起動.bat を右クリック → プロパティ → 一番下の「ブロックの解除
    （許可する）」にチェック → OK → もう一度ダブルクリック
  コマンドが分かる人向けの1行（PowerShell に貼り付け。場所は読み替え）:
    Get-ChildItem -LiteralPath "$HOME\Downloads\TextToVoicevox" -Recurse | Unblock-File

■ Mac の人
  「起動.command」をダブルクリック。これだけです。
  （初回の自動セットアップは Windows と同じ）

  「開けません」と出たら（いちばん確実な道・どの macOS でも）:
    1. ターミナルを開く（Launchpad で「ターミナル」と検索）
    2. bash と打って、半角スペースを1つ入れる
    3. このフォルダの setup_mac.sh をターミナルの画面へドラッグして return
    （ターミナル経由なら Gatekeeper に止められず、実行権限が失われていても
      動きます。検疫フラグを直してから、そのまま起動まで進みます）
  マウスだけでやりたい人は（macOS 15 Sequoia 以降の正規ルート）:
    1. 「開けません」のダイアログを「完了」で閉じる
    2. システム設定 → プライバシーとセキュリティ → 下へスクロール
    3. 「"起動.command"はブロックされました」の隣の「このまま開く」→「開く」
    （むかしの macOS 14 以前なら、右クリック→「開く」→「開く」でも通せます）
  ※最初の1回さえ通れば、起動.command が残りのファイルの検疫フラグと
    実行権限を自動で直します。以降はふつうのダブルクリックで使えます。
  コマンドが分かる人向けの1行（ターミナルに貼り付け。場所は読み替え）:
    bash ~/Downloads/TextToVoicevox/setup_mac.sh

■ zip の解凍について
  Windows = zip を右クリック →「すべて展開」 ／ Mac = zip をダブルクリック
  （OS標準の解凍がおすすめ。一部のツールは日本語ファイル名や実行権限をこわします）

■ うまく動かないとき
  ・「デバッグ起動.bat」（Mac は「デバッグ起動.command」）で起動すると、
    黒い画面にエラーの内容が表示されます
  ・起動に失敗したときは、このフォルダに「起動エラー.log」が残ります
  ・「setup.bat」（Mac は「setup.command」）は部品を入れ直すためのものです。
    ふだんは触らなくて大丈夫（起動.bat / 起動.command が必要なときに自動で呼びます）

■ キャラの立ち絵（画面右のイラスト）について
  紹介画像に写っているキャラの立ち絵は、このアプリに同梱していません。
  （立ち絵はキャラごとに権利者が違うので、勝手に配らないためです）
  assets/立ち絵/ フォルダに「キャラ名.png」（例：ずんだもん.png）を置くと、
  その話者を選んだときに表示されます。入手先・置き方・クレジットの書き方は
  assets/立ち絵/README.md にまとめてあります。無くてもアプリは全部使えます。

■ 音声を作るには
  VOICEVOX 本体を先に起動しておいてください（詳しくは README.md）。

中身はぜんぶ公開しています（MIT・全ソース）:
  https://github.com/rakuyou211-blip/TextToVoicevox
困ったら Issues で教えてください。ちゃんと読みます:
  https://github.com/rakuyou211-blip/TextToVoicevox/issues

落葉い 🍂
