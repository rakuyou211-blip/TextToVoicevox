# 変更履歴

日付は日本時間。バージョンはアプリのタイトルバー（`テキスト抽出 → VOICEVOX vX.Y.Z`）でも確認できます。

## v1.21.0 — 2026-09-24（Windowsで音が鳴らなかった問題・つながりを嘘のないものに）

大きく2つあります。ひとつは、Windowsではアプリの中の音が一度も鳴っていなかったこと。もうひとつは、VOICEVOXとのつながりが「つながっているふり」をしていたことです。

### 鳴らない・遅い・読めない・黙っている

**Windowsで音が一度も鳴っていなかった問題（重大）**

Windowsでは **▶試聴・▶▶連続再生・🔊声を聴く・辞書の読み試聴のすべてが失敗していました**。押すと「うまくいきませんでした…」とだけ出て、理由も分かりませんでした。Mac では問題なく鳴っていたため、長いあいだ気づけずにいました。

原因は、Pythonの音声再生（`winsound`）が「メモリ上の音を、途中で止められる形で鳴らす」ことを**仕様として禁じている**ことでした。このアプリは■停止やEscで途中から止められるようにしたかったので、その組み合わせをずっと使っていて、鳴らそうとした瞬間に必ず失敗していました。

- 音をいったん一時ファイルに書いてから鳴らす方式に変えました（Mac側と同じ考え方）。**途中で止められる性質はそのまま**です
- 一時ファイルは鳴らし終わった時点で必ず片づけます（途中で止めた場合も同じ）
- 音声ファイルの書き出し（🔊音声を生成）は元から問題なく動いていました。今回直ったのは「アプリの中で聴く」機能です

**連続再生が最後の1行しか読まなかった問題**

テキストを抽出した直後や、前回の続きを復元した直後に「▶▶連続再生」を押すと、**最後の1行だけ読んで終わって**いました。

連続再生は「カーソルのある行から最後まで」読む仕様ですが、アプリが本文を入れると**カーソルが自動で最終行の末尾まで送られて**いたためです（テキスト欄の仕様で、文字を入れると入力位置がその末尾まで押し出されます）。つまり毎回「最後の行にカーソルがある」状態から始まっていました。

- 本文を入れ替えたときは**カーソルを先頭に戻す**ようにしました。抽出直後・復元直後・クリップボードOCRの直後、いずれも頭から読みます
- さらに、**「▶▶ 連続再生」は常に本文の最初から読む**ようにしました。本文をクリックして直したあとでも、押せば頭から全部読みます
- 途中から聴きたいときは、**その行を右クリックして「▶▶ ここから連続再生」**を選んでください（新しく足した項目です）。前回の続きから聴く「⏵ 続きから」もこれまでどおり使えます
- 「▶ 試聴」がカーソル行を1行だけ読む動きは変わりません

**押してから音が出るまでを短くしました**

「ボタンを押しても反応が遅い」を測ってみたところ、**画面が固まっているわけではなく**（押した処理そのものは1〜4ミリ秒で終わっています）、待ちはすべてVOICEVOX側の合成時間でした。そのうえで2つ直せる所がありました。

- **アプリを起動して最初の1回だけ、極端に遅く**なっていました（実測2.06秒。2回目からは0.95秒）。VOICEVOXは最初に読み上げを頼まれた瞬間にその声のモデルを読み込むためです。**つないだ時点と、声を選び直した時点で、裏であらかじめ読み込ませておく**ようにしました（実測 **2.06秒 → 0.79秒**）
- **「▶▶ 連続再生」を押しても、最初の1行ができるまで画面が何も言いませんでした。** 1秒ほど無言になるので「押しても反応しない」に見えます。押した瞬間に「読み上げの準備中…（◯行）」と返すようにしました

- **本文でカーソルを置いて手を止めると、その行を裏でこっそり先に作っておく**ようにしました。そのあと「▶ 試聴」を押すと、ほとんど待たずに声が出ます（実測 **2.24秒 → 0.03秒**）。本文を読み込んだ直後の1行目も同じように先回りします
  - 先回りするのは、VOICEVOXにつながっていて、ほかに何もしていないときだけです。音声の生成中や再生中は動きません
  - 200字を超える長い行は先回りしません。頼まれてもいない長い合成でVOICEVOXをふさぐと、かえって試聴が遅くなるためです
  - ちょうど先回り中の行を試聴したときは、同じ合成をもう1本頼まずに、先回りが終わるのを待って使います

残りの時間は、VOICEVOXが実際に声を作っている時間です。VOICEVOX側の設定でGPUモードにできる環境なら、ここはさらに短くなります。

**待たされているあいだ、何も言わなかった所**

- **音声を生成で、合成が100%になったあとの保存が無言でした。** 10時間級の本だと、音声をつなぐだけで数分かかるのに、画面は「ファイルに保存中…」のまま動きませんでした。今は「音声をつないでいます… ◯/◯行」「MP3に変換中…」「字幕を書き出しています…」と、何をしているかを出し続けます
- **PDFを1冊だけ入れて文字を読み取ると、進捗バーが0%から動きませんでした。** 目盛りが「ファイルの数」だったので、1冊だと十数分かかっても0%のままでした。読み取り中はページ数で進むようにし、**残り時間の見当**も出すようにしました

**英語の多い画像が、崩れた文字で読み取られていた問題**

英語の画面を読み取ると「NOT」が「NO丁」、「files」が「創es」のように、漢字まじりの意味の分からない文になっていました。

原因は、WindowsのPCに**日本語の文字読み取り（OCR）しか入っていない**ことでした。英語を頼んでも、アプリは何も言わずに日本語用の読み取りで英文を読んでいたので、英字が似た形の漢字に化けていました。

- **英語も読めるようにしました。** 英文の多い画像だけを、英語の読み取り部品（RapidOCR）で読み直して、よく読めたほうを使います。実物の英語ダイアログで試すと、「the decoy monitor did NOT start.」「Your decoy files are NOT being watched right now.」とそのまま読めました
- Windowsに英語のOCRが入っているPCでは、そちらを先に使います（追加の読み込みがないぶん軽いため）
- 「英語の多い画像か」は、英字の多さではなく**ひらがなの有無**で見分けています。英単語だらけの日本語（技術文書など）には「の」「を」「します」が残るので、誤って英語扱いにはしません。日本語の画像がこの部品に回ることはありません（この部品は日本語のかなを読めないため）
- 読み取り部品は**ネットにつながずに動きます**（読み取りに使うデータは部品の中に入っています）
- **この部品は、欲しい人だけが入れる形にしました。** 入れるとアプリが約240MB大きくなる（ダウンロードは約88MB）ので、英語を読まない人まで重くしないためです。アプリのフォルダの `英語OCRを入れる.bat` を実行すると入ります
- 入っていないPCで英文の多い画像を読んだときは、「Windowsに『英語（米国）』を追加する（アプリは大きくなりません）」と「`英語OCRを入れる.bat` を実行する」の2つの道を案内します
- **アプリの中にも案内を置きました。**「2. ⚙️ 抽出オプション」の右にある **「英語の画像は？」** を押すと、崩れる理由（NOT→NO丁 など）と、いま英語を読める状態かどうか、2つの方法が読めます。そこから Windows の言語設定や `英語OCRを入れる.bat` をそのまま開けます（この窓自体は何もインストールしません）。英文が崩れて読まれたときも、そっけない警告ではなくこの案内が開きます
- 対応は **64bit の Windows・Python 3.12 まで**です（部品が Python 3.13 以降にまだ対応していないため）。Macは最初から日本語と英語を一緒に読めるので、変わりません
- 入れた直後の最初の1回だけ、英語の読み取りの準備に十数秒かかることがあります（Windowsが新しい部品を検査するため。2回目からは一瞬です）
- まだ苦手なもの: ファイルの場所（`C:¥Users¥…`）の記号が崩れやすいこと、画面の端に写り込んだ別の文字（窓の「×」やアイコンの名前）が文に混ざることがあります
- **英語の読み直しを、途中で止められるようにしました。** 英語の多いPDFを読み直している最中にキャンセルを押しても、以前は最大20枚ぶん（1分ほど）止まりませんでした。今は押してから1枚以内で止まります（実測0.2秒）。読み直した分はそのまま使います
- 読み直している間は「英語を読み直しています… 3/10枚（残り◯秒）」と出すようにしました。以前は進捗が止まったまま無言でした
- **英語の読み取りを約2倍速くしました**（1枚あたり実測1.48秒 → 0.84秒）。コアの多いPCで、全部のコアを使おうとしてかえって遅くなっていたためです。メモリの使い方は変わりません
- きれいに読めた英文を、崩れた読み取り結果と比べて誤って捨ててしまうことがあったので、選び方を直しました

**取りこぼしていた足元の不具合**

見た目には出にくいけれど、当たると困る所を7つ直しました。

- **設定ファイルの値がひとつ壊れているだけで、置換ルールと声プリセットが黙って消えていました。** 復元の途中で読めない値に当たると、そこから先の復元が全部飛び、そのまま終了時に「空っぽ」で上書き保存されてしまう作りでした。今は項目ごとに区切って読むので、**読めなかったものだけが既定値に戻り、他はそのまま残ります**
- **.vvproj の保存をキャンセルすると、そのボタンが二度と押せなくなっていました。** 押せる状態に戻し忘れていたためで、VOICEVOXにつなぎ直すまで灰色のままでした
- **処理や再生の途中でアプリを閉じると、書きかけのファイルと一時ファイル（長い本だと数GB）がそのまま残っていました。** 中断を伝えたあと、後片づけが終わるのを**数秒だけ待ってから**閉じるようにしました（応答がないときは待ち続けず、それでも閉じます）
- **止めた再生が、次の再生の音を消していました。** 声を作っている最中に■停止して、すぐ別の行を試聴すると、止めたはずの再生が遅れて音を出そうとして、新しいほうの音をかき消していました（Windowsは同時に1つしか音を鳴らせないため）。止められた再生は、もう音を出さないようにしました
- **アプリが途中で落ちたときの作業用ファイルが、ずっと残っていました。** 普通に閉じれば片づきますが、強制終了や停電ではそのまま残ります（実際に13MBが1週間残っていました）。起動したときに、1日以上前の残りものを片づけるようにしました。いま動いている作業には触りません
- **サブモニタに置いた窓が、次の起動でメイン画面へ引き戻されていました。** 最大化して閉じても、次は普通の大きさで開いていました。どちらも前回のまま開くようにしました（外したモニタの位置だったときだけ、メイン画面に寄せます）
- **停止したあとすぐ次を再生すると、前の再生の後始末が新しい再生に割り込んでいました。** 鳴っているのに「待機中」の表示に戻り、■停止ボタンまで消えて**音を止められなくなる**ことがありました。今は前の再生からの知らせを見分けて捨てるので、割り込まれません

**エラーの原因が分かるようにしました**

- うまくいかなかったとき、詳しい内容をアプリのフォルダの **`エラー.log`** にも残すようにしました。これまではダイアログを閉じると消えてしまい、後から調べようがありませんでした（大きくなりすぎないよう、古い分から自動で捨てます）
- 「音を鳴らせなかった」「VOICEVOXが読み上げを断った」「音声データが壊れていた」を、それぞれ違う文言で伝えるようにしました。どれも次にすることが違うためです

### VOICEVOXとのつながり

**気づけないまま損をする**作りになっていた所を、まとめて直しました。

**「接続OK」と出ているのに、実は切れている問題**

VOICEVOXを閉じても、アプリの表示は「● 接続OK」のままでした。ボタンも押せる状態で残っていて、押して初めて「つながっていなかった」と分かる——という順番だったのを、逆にしました。

- 接続中は**15秒おきにエンジンの生きているかを確かめ**、切れていたら「⚠ 接続が切れました」に変わり、音声まわりのボタンも押せなくなります（実測で約20秒以内に気づきます）
- 切れたことに気づいた後、**もう一度VOICEVOXを起動すれば自動でつなぎ直します**。これまでは一度つながると内部の判定が居座ってしまい、2回目以降は自動で再接続できませんでした
- 再接続のとき、**その場で選び直していた話者がそのまま残ります**（これまでは保存済みの話者へ黙って巻き戻っていました）
- 見張りは「つながっているとき」だけ動きます。VOICEVOXを使わずテキスト抽出だけしたい人の裏で、通信し続けることはありません（最小化中・処理中も止まります）

**エディタ画面を開かずに、読み上げエンジンだけを起動できるようにしました**

VOICEVOXには、エディタ画面（Electron製で200MB以上あります）とは別に、読み上げだけを担当するエンジンが同梱されています。読み上げるだけならエディタ画面は要らないので、**エンジンだけを起動する選択肢**を足しました。

- 接続の欄に「**エンジンのみ**」ボタンを追加しました。これまでどおりの「VOICEVOX起動」も残してあるので、あとで手直ししたい日はそちらを使えます
- エンジンだけの起動は軽く、実測で**3秒ほどでつながります**
- **このアプリを閉じると、自分で起動したエンジンも一緒に終了します。** ただし、あなたが自分で開いたVOICEVOXには絶対に手を出しません（アプリを閉じてもそのまま残ります）
- すでにVOICEVOXが動いているときは二重に起動せず、そのままつなぎます
- 使いたいポートが他のソフトに取られていたら、空いているポートを自分で選んで起動します

**VOICEVOXが見つけられなかった問題**

これまでは決まった2か所しか探していなかったので、Dドライブなど別の場所に入れている人は「VOICEVOX起動」ボタンが永久に使えませんでした。

- Windowsのインストール情報からも探すようにしました（標準以外の場所に入れていても見つかります）
- `Program Files (x86)` や固定ディスクの分かりやすい場所も見ます（USBやネットワークドライブは、待たされるので触りません）
- Macでは Spotlight にも聞きます
- それでも見つからないときは、**場所を手で選べる**ようにしました。一度選べば次からは覚えています

**VOICEVOXが別のポートで動いていると、永久につながらない問題**

VOICEVOXは、使おうとしたポート（50021番）が他のソフトに使われていると、**黙って別のポートへ移ります**。アプリ側は50021番だけを見ていたので、この場合いくら待ってもつながりませんでした。

- VOICEVOX本体が「今どのポートで動いているか」を書き出しているファイルを見て、**別のポートに移っていても自動で見つける**ようにしました。実際に50021番を塞いだ状態で試し、50022番へ移ったVOICEVOXに自動でつながることを確認しています
- 別のポートにつないだときは、**URL欄にその値を出して「別のポートで動いていたのでこちらにつないだよ」と伝えます**（黙って別の場所につなぐのは不親切なので）
- ただし**自分でURLを変えている人には、この自動切り替えをしません**。設定を勝手に奪わないためです
- VOICEVOXを終了してもこのファイルは残るので、「書いてあるからつながっている」とは判断せず、必ず生きているか確かめてから使います

**「未接続」の一言で片づけていた問題**

つながらない理由には「VOICEVOXが起動していない」「起動中でまだ応答できない」「URLの書き方が違う」「そのポートは別のソフトが使っている」など、対処のまるで違うものが混ざっていました。それを全部「未接続」と表示していたので、何を直せばいいのか分かりませんでした。

- 理由ごとに違う文言を出すようにしました。URLの形が違うときは書き方の例も添えます
- **`http://` を書き忘れても通る**ようにしました（補ったURLは欄にも書き戻すので、何が使われているか見えます）
- アプリとVOICEVOXを同時に起動したときのために、**起動から20秒間は静かに再接続を試みます**（VOICEVOXが今まさに起動したところなら60秒）。その時間を過ぎたら完全にやめるので、VOICEVOXを使わずテキスト抽出だけしたい人の裏で通信し続けることはありません

**調整した声が、エディタに伝わらない問題**

話速・音高・抑揚・音量をアプリで調整して `.vvproj` に保存しても、VOICEVOXエディタで開くと全ブロックが既定値に戻っていました。調整の情報がファイルに書かれていなかったためです。

- **調整した内容をプロジェクトにも書き込むようにしました。** エディタで開くと、アプリと同じ声の設定から続きを触れます
- 「調整値も入れる」のチェックを、プロジェクト保存ボタンの隣に置きました（既定でオン）
- **調整していないときは、これまでと1バイトも変わらないファイルを、待ち時間ゼロで書き出します。** 引き継ぐ情報が無いときにエンジンへ問い合わせても意味がないためです
- 調整しているときは行ごとにエンジンへ読みを問い合わせるので、進み具合を表示し、途中でキャンセルできるようにしました
- 途中でVOICEVOXが落ちるなどして読みを取れなかった場合は、**その行だけ調整値なし**にして保存を続けます。全部取れなかったときは、これまでどおりの形で保存します。「開けないファイルを書き出す」よりも「調整値を諦めてでも確実に開けるファイルを書き出す」を優先しました
- 書き出す直前に、こちら側で形を点検してから保存します（エディタに「開けません」と言われても原因が見えないため）

**読み方辞書が、黙って設定を巻き戻していた問題**

VOICEVOXエディタ側で「品詞は動詞」「優先度は8」と決めた単語を、このアプリで**読みだけ直すと、品詞と優先度が既定値に戻っていました**。読みを直しただけのつもりなので、後で読み方が変わっても原因にたどり着けません。

- 辞書の一覧に**品詞と優先度の列**を足し、フォームでも設定できるようにしました。単語欄に入れた単語が登録済みなら、**その単語の今の設定が自動でフォームに入ります**（一覧をダブルクリックしたときも、本文を右クリックして「読み方辞書に登録」から来たときも同じ）。画面に出ている値がそのまま適用されるので、読みだけ直して上書きしても**元の品詞・優先度がそのまま保たれます**
- 新しい単語のときは自動で入れ替えたりしないので、自分で選んだ品詞・優先度はそのまま使われます
- 「同じ単語が既に登録されているか」の判定を、**画面の一覧ではなくVOICEVOXの実データに問い合わせて**行うようにしました。エディタ側で先に登録していた単語をこのアプリから追加しても、**二重に並ばなくなります**
- 全角と半角の違い（`ABC` と `ＡＢＣ`）も同じ単語として扱います。VOICEVOXは登録時に表記をそろえるため、そのままでは同じ単語を二重に登録してしまっていました
- 辞書の窓に戻ってきたとき、自動で最新の内容を読み直します（エディタ側で編集してからこのアプリに戻る、という流れで食い違わないように）
- 辞書の書き出しに品詞と優先度も含めるようにしました。ファイルの形（リスト）は変えていないので、**古いバージョンのアプリでもこれまでどおり読み込めます**

**その他**

- プロジェクトに書き込むエンジンの識別子を、**実際につながっているエンジンから取る**ようにしました。公式のVOICEVOXでは値が同じなので見た目は変わりませんが、別のエンジンを繋いだときに正しくなります
- プロジェクト保存が、再生中・処理中には押せないようになりました（これまでは押せてしまっていました）
- プロジェクトの書き込みを、途中で電源が落ちてもファイルが壊れない方式に変えました

## v1.20.0 — 2026-08-08（最初のダブルクリックで、つまずかせない）

配布zipを別のパソコンでダウンロードして開くと、Windows の SmartScreen や Mac の Gatekeeper に止められて、**最初の起動までたどり着けない**ことがありました。署名なしの個人配布ではどうしても警告は出ますが、「通すのは最初の1回だけ」で済むようにしました。

**Windows**
- `起動.bat`・`setup.bat`・`デバッグ起動.bat` が実行のたびに、**ダウンロード由来の警告ブロック（Mark of the Web）を自動で外す**ようにしました（起動系はフォルダ直下だけを素早く、setup はフォルダ全体を徹底的に）。SmartScreen の「詳細情報」→「実行」は、最初にダブルクリックする `起動.bat` の1回だけで済みます
- NAS など UNC パス上で実行された場合に無関係なフォルダを走査しないよう、解除の対象フォルダは明示して渡しています（解除に失敗しても起動は続行する防御的な作りです）

**Mac**
- `起動.command`・`setup.command`・`デバッグ起動.command` が実行のたびに、フォルダの**検疫フラグ（com.apple.quarantine）を外し、実行権限(+x)を直す**ようにしました。Gatekeeper の承認（macOS 15以降: システム設定→プライバシーとセキュリティ→「このまま開く」／14以前: 右クリック→「開く」）は初回の1回だけで済みます
- **`setup_mac.sh` を新設**：Gatekeeperに止められたとき・「アクセス権がありません」と出るときの、いちばん確実な入口です。ターミナルに `bash ` と打ってこのファイルをドラッグ → return で、検疫と権限を修復してからそのまま起動（初回は自動セットアップ）まで進みます。bash 経由の実行は Gatekeeper のブロック対象外で、実行権限が失われていても動きます（macOS 15 Sequoia で「右クリック→開く」の迂回が廃止されたため、この道を第一の案内にしています）

**zip・ドキュメント**
- 同梱の **`はじめにお読みください.txt`** に、Windows／Mac 別の「開けないとき」の最短手順を統合しました（コピペできる1行コマンド付き。zip は OS 標準の解凍がおすすめ、という注意も）
- README（日英）の「はじめての人へ（警告が出たら）」を新しい仕組みに合わせて書き直しました。macOS 15 以降で「右クリック→開く」が使えなくなっている件も反映しています
- 検疫フラグ付きの zip を実際に作って再現テストし、`bash setup_mac.sh` 一発で（検疫29ファイル→0・実行権限の復元・セットアップ完走まで）通ることを確認しました
- 自己修復スクリプト・実行ビット・案内テキストの BOM/改行コード・README の整合を `tests/test_docs.py` のテストで固定しました（うっかり壊すとテストが落ちます）。※`tests/test_docs.py` は現在ありません。README の整合は、いまは自動では確かめていません

なお、**EXE化・コード署名・公証は今回もしていません**（署名なし個人配布の前提で、できる範囲の最善を尽くした、という変更です）。

## v1.19.1 — 2026-07-27（「ダブルクリックしても何も起きない」をなくす）

「起動できない」と迷子になる入口をふさぎました。中身の機能は変わっていません。

- **起動.bat／起動.command だけで動くように**：これまで初回は setup → 起動 の2段構えで、順番を知らないと「venv not found」で止まっていました。起動側がセットアップ未了に気づいたら、その場で setup を自動実行してからアプリを開きます（初回だけネットが要るのは従来どおり）。フォルダごと別のパソコンへ持ってきて venv が動かなくなっている場合も、自動で作り直します
- **「はじめにお読みください.txt」を同梱**：フォルダに起動系ファイルが6個並んでいて最初にどれを押せばいいか分かりにくかったので、道しるべを1枚置きました（答え：起動.bat か 起動.command、それだけ）
- **起動失敗が無言にならないように**：起動.bat はコンソール無し（pythonw）で立ち上げるため、起動中に何か失敗すると今までは画面もメッセージも出ない「無反応」に見えていました。これからは失敗内容を「起動エラー.log」に保存し、ダイアログでもお知らせします
- **ドラッグ＆ドロップ部品が壊れた環境でも本体は開くように**：tkinterdnd2 のネイティブ部品（tkdnd）が読み込めない環境（ARM64版Windows等）では、D&D無効で一度だけ自動でやり直します（ファイルは従来どおり「選ぶ」ボタンから使えます）
- **デバッグ起動.bat にも道案内**：セットアップ前に実行したとき、Windowsの生エラーではなく日本語の案内が出ます

## v1.19.0 — 2026-07-18（立ち絵が無い子にも、そっと道案内を）

前回 v1.18.0 で全キャラの立ち絵に対応しましたが、使ってみると小さな不親切が見つかったので手当てしました。

- **立ち絵が未設定のキャラを選んだとき、画面が無言で空になっていた**のを、「『◯◯』の立ち絵はまだありません。`assets/立ち絵/` に `◯◯.png` を置くと表示されます」と**案内を出す**ようにしました。何を置けば出るのか、画面で分かります（別キャラを誤って出すことはしません）。
- 声・調整の行にボタンを詰め込みすぎて**音量欄が隠れていた**のを直しました。試作していた「立ち絵の一括取り込み」ボタンは、この窮屈さの原因になったため取り下げています（立ち絵は従来どおり `assets/立ち絵/` に置く方式のままです）。

立ち絵の入れ方・全43キャラの命名・各キャラのガイドラインは [`assets/立ち絵/README.md`](assets/立ち絵/README.md) にまとめてあります。

## v1.18.0 — 2026-07-18（立ち絵が全キャラ対応・試聴の無反応対策）

**立ち絵が全43キャラ対応に**
- これまで立ち絵は **ずんだもん／四国めたん の2キャラだけ**でしたが、VOICEVOXの**全キャラ**に対応しました。`assets/立ち絵/` に **`キャラ名.png`**（例 `春日部つむぎ.png`・`九州そら.png`）を置くだけで、その話者を選んだときに立ち絵が出ます（まばたき・口パクの差分 `_closed`/`_open`/`_blink` も同じ命名で対応）
- **選んだ話者の立ち絵が無いときは、別キャラを誤って出さず枠を空に**します（連続再生で話者が変わっても、立ち絵のあるキャラだけ正しく表示・口パク）
- **ファイル名に使えない文字を含むキャラも自動で照合**（`小夜/SAYO`→`小夜SAYO.png`、`†聖騎士 紅桜†`→空白を除いて保存）。旧 `zundamon.png`/`metan.png`（ローマ字）もそのまま使えます
- 立ち絵はキャラごとに権利者が違うため、画面のクレジットを汎用表記に変更。各キャラのガイドラインの確認方法・置き方は [`assets/立ち絵/README.md`](assets/立ち絵/README.md) にまとめました（**立ち絵は引き続き同梱しません**）

**試聴まわりの堅牢化**
- 試聴・連続再生を押しても無反応に見えるケースを解消：再生中に押したら理由を表示、未接続なら「VOICEVOXを起動してね」の具体案内、■停止で状態が戻らないときの強制復帰を追加

## v1.17.0 — 2026-07-18（10時間の本に耐える・途中でやめても無駄にならない）

**長編対応（今回の目玉）**
- **メモリがほぼ一定に**：音声生成が全行のWAVをメモリに溜めてから結合する方式（10時間級の本で数GB）から、行ごとにディスクへ書いて逐次結合する方式（常に数MB）に。**24時間超の全巻結合はWAVの4GB上限を保存前に検知**して分割かM4A/M4Bを案内します（従来は全行合成後の保存で全損でした）
- **キャンセルしても「ここまでを保存」**：全文結合の生成を途中でやめたとき、合成済みの部分を「◯◯_途中まで.m4a」として保存できます（章・字幕付き。従来は成果ゼロでした）
- **1行の失敗で全巻が全損しない**：エンジンの一時不調は行ごとに2回まで自動リトライ。連続再生は失敗行をスキップして読み続けます（連続3失敗＝エンジン停止のときだけ従来どおりエラー）
- **キャッシュが長編で効くように**：生成中に自分の前半行を追い出す自己破壊を防止（実行中エントリの保護）。上限も変更可能に（§4「キャッシュ…」から。10時間級を常用なら2000MB推奨）
- **OCRの途中キャンセルとページ進捗**：数百ページのスキャンPDFでもキャンセルが数秒で効き、「OCR実行中... (37/300枚)」と実際の進捗が見えます（従来はOCR全体が終わるまで待たされました）。外部プロセス（Windows OCR・音声変換）にはタイムアウトを設定し、固まってもUIが永久busyにならないように

**新機能**
- **📄 PDFのページ範囲指定**：§2「ページ:」欄（例 `5-320` / `1-3,10-`）で表紙・目次・索引を読み飛ばせます（CLIは `--pages`。全角数字・「、」「〜」もOK）
- **🗂 セリフ一覧CSV**：分割出力時に「セリフ一覧.csv」（ファイル名・話者・テキスト・長さ秒）を自動保存。動画編集ソフトでの素材整理の対応表に（Excel対応のBOM付きUTF-8）
- **🏷 掛け合いのファイル名に話者名**：複数話者のとき `001_ずんだもん_こんにちは.wav` 形式に（ファイル名だけで誰の声か分かる）
- **✅ @話者タグの事前検証**：タイプミスで解決できないタグを合成前に指摘し、該当行へジャンプできます（従来は全編を聴き直すまで気づけませんでした）
- **🧹 キャッシュ管理**：§4接続行の「キャッシュ…」から容量確認・上限変更・クリア・フォルダを開くが1画面で
- **📊 行数・文字数・読み上げめやすの常時表示**（§3右上。＃メモ行・@タグは除いて計算）
- **CLIにも合成キャッシュ**（`--no-cache` で無効）。台本を1行直して再実行→未変更行は即完了

**直したところ**
- 分割出力が999ファイルを超えると連番が4桁になり名前順ソートが崩れていた（総数に応じた桁数に）
- 数値欄が空のまま「プリセット保存」すると無反応で保存されなかった
- M4B選択時に「N行」欄が編集できたが値は無視されていた（灰色に）
- 辞書ハッシュの取得失敗時、二度と使われないキャッシュファイルを書き続けていた（キャッシュ汚染）
- 試聴・声サンプル再生中に「⏵ 続きから」が押せて画面だけ飛んでいた
- READMEの版数が4リリース前（v1.11.0）のまま取り残されていた → **版数とドキュメントの整合をテストで強制**（tests/test_docs.py 新設。今後は更新漏れのままコミットするとテストが落ちます）。英語READMEにM4B・声サンプル・読み確認等の欠落機能を追記し、最低Pythonバージョン表記を3.9に統一

**内部の見直し**
- M4Bチャプター構築・出力グループ化・OCR補正をGUI/CLI共有のcore関数へ統合（片方だけ実装される事故の防止）
- メモ行判定の残っていたコピペ2箇所を core.is_memo_line に統一

**リリース前レビューで見つけた取りこぼしの修正**
- 音声生成のスプール作成がディスクフルで失敗すると、操作不能（永久busy）になっていた → エラー表示して復帰するように
- キャッシュ上限を下げて「適用」しても超過分が削除されず表示も矛盾していた → 即座に削除し、起動時にも前回の超過分を掃除
- @話者タグ確認の「いいえ」で飛ぶ行が、本文先頭に空行があるとずれていた → 正しい行へジャンプ
- キャンセル時の部分保存で連結に失敗すると書きかけの `.part` が残っていた → 残さないように

## v1.16.0 — 2026-07-18（声選びが速く・直した行だけ再合成・壊れない保存）

**新機能**
- **🗣 話者選択が2段に**：170超のスタイルが1本に並んでいたコンボを「キャラ → スタイル」の2段に分割。キャラを選んで「🔊 声を聴く」でスタイルを聴き比べる、が自然にできます
- **⚡ 行単位の合成キャッシュ**：同じ行・同じ声・同じ設定の合成結果を自動で再利用（上限500MB・古い順に自動削除）。**誤読を1行直して再生成→他の行は即完了**、連続再生で聴いた本の音声生成もほぼ一瞬に。辞書やエンジンを変えると自動で作り直します
- **⛔ テキスト抽出のキャンセル**：数百ページのスキャンPDFのOCRを途中で止められます（抽出中はボタンが「⛔ キャンセル」に。Escでも可・そこまでの部分結果は表示）
- **■ 試聴・声サンプルも止められる**：長い行の試聴を「■ 停止」/Escで途中停止できます（従来は最後まで待つしかありませんでした）
- **📖 しおりの可視化**：「⏵ 続きから」で再開する行が淡紫色でマークされ、どこまで聴いたかが一目で分かります
- **📚 M4B: 章見出しが無い本にも自動チャプター**（約10分ごと・区切り行の本文冒頭がタイトルに）。Apple Books等で長い本の頭出しができます
- **📕 読み方辞書の強化**:「追加/上書き」（同じ単語は重複せず上書き）・一覧ダブルクリックで編集・「▶ 読みを試聴」で登録した読みをその場で確認
- **🖱 右クリックに「元に戻す/やり直す」**（WindowsはCtrl+Yのやり直しも）
- **⌨ 入力ファイルのキーボード操作**：Delete=削除・Alt+↑↓=並べ替え・選択でフルパス表示（同名ファイルの区別に）

**CLI**
- **--unit each/combine/nlines/para**（GUIの「まとめ方」と同等。--split-lines N・--keep-blank も）と、分割出力でも --srt でファイルごとに字幕保存
- **--vvproj**：台本txtからVOICEVOXプロジェクトを一括生成（行別@話者タグ対応）
- **合成が3並列に**（GUIと同じ。長編で約3倍速）・**--no-preprocess**・**--name-snippet**（001_本文冒頭.wav をオプトインで）・末尾スラッシュ付きURLでも接続できるように

**テキストの精度**
- **《》の誤削除を修正**：なろう/カクヨム系の「彼は《ファイアボール》を放った」のスキル名・強調が消えていたのを、ルビ（漢字直後・｜指定）だけ削除するように限定。**カクヨム傍点《《…》》も正しく本文へ展開**（従来は本文が消えて「》」が残る破損でした）
- **ノイズ除去をOCR由来テキスト限定に**：小説txt中の英文の引用行・年号だけの行が「画面キャプチャのノイズ」として消えていたのを修正（txt/docx/EPUB・PDFテキスト層には触れません）
- **PDF「自動」の改善**：透かし（Scanned by …）やページ番号だけのテキスト層があるスキャンPDFで、本文が丸ごと失われていたのをOCRと比較して良い方を採用するように
- **Word（.docx）のテキストボックス**：同じ文言が最大4回読み上げられていた重複抽出を修正
- **EPUB**：目次ページ（nav）と linear="no" の付録を本文から除外（全章が対象になる誤ラベルEPUBは従来どおり全文）
- **EUC-JPのテキストに対応**：文字コード判定をスコア方式にし、古い配布txtの文字化け読み上げを解消
- **半角カナ→全角**：ｶﾞｲﾄﾞ→ガイド・半角句点｡でも文分割が効くように（「全角英数→半角」ON時）
- **数値レンジ「10〜20」→「10から20」**・組文字単位の読み展開（㌔㌢㍉㌘㌧㌫㌍㌦㍗・㍾㍽㍼㍻㋿）

**直したところ（設定・データを守る修正）**
- **設定消失バグ3件を修正**：数値欄（話速・DPI等）を空にしたまま閉じると settings.json が0バイトに壊れて全設定（プリセット・置換ルール・テーマ・しおり）が消えていた／エンジン未接続で閉じると話者選択が空文字で上書きされていた／どちらも空欄は既定値へフォールバックし、保存はアトミック書き込み（書き込み途中のクラッシュ・電源断でも壊れない）に。壊れた設定は .bak に退避
- **UI永久ロック2件を修正**：数値欄が空のまま「▶ 試聴」「▶▶ 連続再生」を押すと全操作が拒否され再起動しかなくなっていた
- **UIフリーズの単一障害点を解消**：内部メッセージ処理の例外でUI全体が固まる構造を、1件落として続行する方式に
- 生成中の Ctrl/Cmd+G が無警告で生成をキャンセルしていた（生成中は無効に。キャンセルはEsc/ボタンで明示的に）
- 選択範囲内の右クリックで「この行を試聴」「@タグ挿入」がクリックした行と別の行に効いていた
- ＃メモ行トグルが選択終端の行頭（未選択の行）まで巻き込んでいた
- 検索で最初に「↑前」を押すと末尾でなく末尾から2番目に飛んでいた
- 辞書JSONの読み込みでファイル内の重複単語がそのまま重複登録されていた
- パレットPNG/GIFの拡大縮小が低品質（ニアレストネイバー）でOCR精度を下げていた
- 一括置換・全ルール適用で表示が先頭へ飛んでいた（カーソル・スクロール位置を保持）
- VOICEVOX起動待ちの自動接続でボタンが3秒ごとに明滅していた（静かに確認する方式に）
- 実行中にウィンドウを閉じると書きかけのファイルが壊れたまま残っていた（確認ダイアログを追加）
- 分割出力（001_○○.wav）と結合時SRTが無確認で上書きされていた（事前に件数を確認）

**見直し・削除**
- 「N行」「文間の無音」の欄は、効かないまとめ方のときは灰色に（触れるのに無視される欄を排除）
- 試聴の内部往復を削減（audio_query 1回で合成と読み確認を両方）
- 未使用コードの削除（denoise内部の不使用関数・OCRヘルパーの不要引数・重複した色設定）

## v1.15.0 — 2026-07-18（右クリックで次の一手・仕上がりに手が届く）

**新機能**
- **🖱 本文の右クリックメニュー**：切り取り/コピー/貼り付け/すべて選択に加えて、この画面でよくやる操作への近道を追加
  - **▶ この行を試聴**：クリックした行をその場で聴ける
  - **「◯◯」を読み方辞書に登録...**：誤読を見つけたら選択→右クリックですぐ辞書へ（単語欄に自動入力）
  - **この行を別の話者で読む（@タグ挿入）**：話者名のサブメニューから選ぶだけで行頭に `@話者名: ` を挿入（既存タグは置き換え・「タグを外す」も）
  - **＃メモ行にする/解除**：選択した複数行をまとめて読み上げ対象から外せます（台本のコメントアウト）
- **🎉 完了ダイアログに「📂 保存先を開く」「📋 クレジットをコピー」**：生成後にFinder/エクスプローラーへ直行でき、公開時に必要なクレジット表記（`VOICEVOX:話者名`）もワンクリックでコピーできます（表記漏れ防止・VOICEVOX利用規約準拠の後押し）
- **💾 本文の自動保存**：60秒ごとに本文を自動保存（従来は終了時のみ＝クラッシュや強制終了で編集が消えていました）。復元は従来どおり次回起動時
- **🔌 VOICEVOX起動→自動でつながる**：「VOICEVOX起動」を押したら、エンジンの準備ができ次第自動で接続します（従来は起動→手動で「エンジン接続確認」の2ステップ）。待機中は「起動を待っています…」と表示
- **📥 入力ファイルの並べ替え（▲/▼）**：ファイルの順序＝抽出・結合の順序なので、複数ファイルの本を正しい順に直せます（複数選択のまま移動可）
- **📁 分割出力のファイル名に本文の先頭を付与**：`001.wav` → `001_こんにちは今日はい.wav`。掛け合い動画の素材整理などで、どのファイルがどのセリフか一目で分かります（連番・字幕の対応は従来どおり。CLIは自動化互換のため従来の連番のみ）

**見直し**
- **読み上げめやすの精度**：＃メモ行と行頭の@話者タグは読まれないので、所要時間の概算からも除外するように（台本ではめやすが長く出すぎていました）
- 自動保存の書き込みは本文が変わったときだけ（無駄なディスク書き込みをしない）

**削除**
- 未使用の内部API `toggle_theme` を削除（テーマ切替はv1.11.0からプルダウンに統合済み・動作への影響なし）

## v1.14.0 — 2026-07-18（誤読に気づける・写真に強くなる）

**新機能**
- **🔍 読み確認**：`▶ 試聴` すると「読み『ダイ イッショオ、コンニチワ…』」と**VOICEVOXが実際にどう読んだか**をカナで表示。誤読（例: O157→オオイチゴオナナ）に気づいたら「読み方辞書...」で直せます
- **⏱ 読み上げ時間のめやす**：抽出完了時に「読み上げめやす12分」のように所要時間の概算を表示（話速設定を反映）
- **＃メモ行**：行頭に `#` を付けた行は読み上げ・音声生成・vvproj出力からスキップ。台本のメモや指示書きに使えます
- **URL・メールを読み飛ばす（既定ON）**：読み上げると1文字ずつ読まれてしまうURL・メールアドレスを本文から除去（詳細設定でOFF可）

**画像認識の精度 第3弾**
- **スマホ写真の向きを自動補正**：EXIFの回転情報を反映（今までは横向き写真がほぼ読めないことがありました）
- **回転リトライ**：ほぼ読めなかった画像は90°/270°回転でも再OCRし、一番読めた結果を採用（横倒しスキャン・写真対策。macOS）
- **PDFページも自動再OCRの対象に**：影・ムラのあるページはページを描画し直してリトライ（今までは単体画像のみ）

**テキストの精度**
- 数字の桁区切りを除去：`1,234円`→`1234円`（「いち、にさんよん」と誤読されるため。「全角英数→半角」ON時）
- 中黒の連続 `・・・` → `…` に統一（同上）
- OCR誤字補正に `ハ→八` を追加（`十ハ番`→`十八番`。「ハイテク」等の実在語は保護）

**CLI**
- `--format m4b`（章付きオーディオブック）・`--strip-urls`（既定ON）を追加。＃メモ行・クレジット表記の案内もGUIと同様に対応

**見直し**
- M4B選択中は「まとめ方」を無効表示に（M4Bは常に全文結合のため）
- `▶ 試聴` 中もいま読んでいる行をハイライト表示（連続再生と同じ見た目に）

## v1.13.0 — 2026-07-18（声を選んで、本として聴ける）

**新機能**
- **🔊 声サンプル試聴**：話者コンボの隣の「🔊 声を聴く」で、選択中キャラの**公式ボイスサンプル**をワンクリック再生。声選びに迷わなくなりました（エンジン同梱の公式音源をアプリ内試聴のみに使用）
- **📚 オーディオブック出力（M4B・章付き）**：音声形式に **M4B** を追加。全文を1ファイルに結合し、本文の章見出し（`第◯章`・`プロローグ` 等）を自動検出して**チャプター**を埋め込みます。Apple Booksやブックプレーヤーアプリで「本」として頭出しできます（純Python実装・AVFoundationでの読み戻し検証済み。章見出しが無い場合は章なしで保存）
- **⛔ 音声生成のキャンセル**：生成中は「🔊 音声を生成」が「⛔ キャンセル」に変わり、長い本の生成を途中で止められます（Escキーでも可）
- **⏸ 連続再生の一時停止/再開**：停止すると頭からだった連続再生に一時停止を追加。**スペースキー**でも切替でき、再開すると同じ行の頭から読み直します
- **📋 整形レポート**：ノイズ除去で消えた行とOCR誤字補正の内容を§3の「レポート」ボタンで確認可能に。誤って消えた本文のコピー復元や、補正のON/OFF判断に使えます

**OCRの精度 第2弾**
- **2段組の読み順を修正**：段組PDFのOCRで左右の行が交互に混ざっていたのを、段組と確信できる場合のみ列単位（左→右）で読むように（誤検出防止のガード付き）
- **写真の自動再OCR**：影や照明ムラでほぼ読めなかった画像は、照明を平坦化して自動でもう一度OCRし、良い方を採用（macOS・低品質時のみ実行で通常は速度影響ゼロ）
- **OCR失敗が見えるように**：失敗理由と「文字を検出できなかった枚数」を抽出後の警告に表示（従来は無言で空になっていました）

**公式規定の遵守を明示**
- 音声生成の完了ダイアログに、公開時に必要な**クレジット表記（`VOICEVOX:話者名`）を毎回表示**。M4Bにも同じ案内を含めます（VOICEVOX利用規約・各キャラクター利用ガイドライン準拠。サンプル音声・立ち絵などの公式素材は引き続き同梱しません）

**UI・人間味**
- **接続まわりをすっきり**：エンジン接続後はURL欄・接続ボタンを畳んで「**● 接続OK (vX)**」（テーマ対応の緑）＋「接続設定…」だけに。失敗時は自動でフル表示に戻ります
- **エラーが人間の言葉に**：生のエラーコードの代わりに「VOICEVOXが起動しているか確認してね」のような**原因のヒントと次の一手**を表示。技術的な詳細は「詳細を表示」を押したときだけ（不具合報告にはこちらを）
- **あいさつと道案内**：起動時に時間帯のあいさつ（おはよう☀️/こんにちは🍵/こんばんは🌙）、初回起動では3ステップの使い方を案内
- **節目のことばを温かく**：「✅ 抽出できたよ」「🎉 音声ができたよ！」「📖 最後まで読み終わったよ、おつかれさま！」。残り2分を超える長い生成には「のんびり待っててね☕」を添えます

## v1.12.0 — 2026-07-18（OCRがかしこく・文が自然に・次に押すボタンが分かる）

**OCRの精度（写真・スクショ・スキャンPDF）**
- **WindowsでもmacOSと同じ“座標つきOCR”に**：`ocr_win.ps1` が行の外接矩形を返すようになり、これまでmacOS限定だった **折り返し連結**（割れた1文のつなぎ直し）と**映像内ラベルの除去**（局ロゴ・番組名・日時・カテゴリ）がWindowsでも効きます（旧形式との互換維持・傾き検出時は安全側で従来動作）。※Windows実機での実画像確認は次回リリースまでに実施予定
- **Windowsの“無言でOCR空振り”を修正**：Windows OCRエンジンの上限（長辺2600px）を超える画像（300dpiのA4スキャン等）が例外→空文字になっていたのを、前処理とPowerShell側の両方で上限内に収めるよう修正
- **前処理の見直し**：透過PNG（ウィンドウ撮影・クリップボード画像）が黒く潰れてOCRを阻害していたのを白背景に合成。macOSではグレースケール強制をやめ**カラーのままApple Visionへ**（NN系はカラーで精度が出る）。Windowsの強調はハローの少ないUnsharpMaskに変更。「画像前処理」OFFが本当に無加工（サイズ上限のみ）になるよう修正
- macOSのVision OCRで日本語対応リビジョン（Revision 3）を明示指定（環境差で古い版に落ちる事故を防止）
- クリップボードOCRの一時画像が `%TEMP%` に残り続けていたのを修正（OCR後に必ず削除）

**テキストの精度**
- **OCR誤字の文脈補正（新設・既定ON）**：`サ一ビス`→`サービス`、`卜ヨタ`→`トヨタ`、`二ュース`→`ニュース`、`入カ完了`→`入力完了`、`2O26年`→`2026年` など、同形文字の取り違えを前後の文字種で確定できるときだけ補正。「数カ月」「口コミ」「口パク」「二ヶ月」「O157」「H2O」などの実在語はガードで保護（迷ったら触らない）。**OCR結果にだけ**適用され、txt・docx・EPUB・PDFテキスト層には触れません（§2「OCR誤字を補正」でOFF可・CLIは `--no-fix-confusables`）
- **「」（）の内側で文を切らないように**：`「もう帰る。」と彼は言った。` が2つに割れて不自然なポーズ・字幕になっていたのを1文のまま合成。`えっ！？` の連続記号も1文に
- **読めない記号の展開**：「全角英数→半角」ONのとき、`①`→`1`・`Ⅲ`→`3`・`㈱`→`株式会社`・`50㎡`→`50平方メートル`・`25℃`→`25度`・`№`→`ナンバー` などを読みに展開（※既にONで使っていた方は挙動が少し変わります）
- **ノイズ除去の改善**：`写真：ロイター` 等のクレジット行と `1.2万人` 等の視聴数オーバーレイを除去対象に追加。逆に日本語記事中の `iPhone 17 Pro Max` のような英数の製品名見出しが誤って消えていたのを保護
- **青空文庫の対応強化**：踊り字（`こゝろ`→`こころ`・`つゞく`→`つづく`）とくの字点（`どき／＼`→`どきどき`）を読める形に展開。末尾の「底本：…」と冒頭の記号説明ブロックを読み上げから除去

**ボタン配置・操作**
- **「次に押すボタン」がひと目で分かる**：本文が空のときは `▶ テキスト抽出 実行`、本文ができたら `🔊 音声を生成` だけがオレンジ（Primary）になります
- **押せないボタンが理由を教える**：無効中の `🔊 音声を生成`・`▶ 試聴`・`プロジェクト保存` などにマウスを乗せると「エンジン未接続です。接続確認を押すと使えます」等の理由をツールチップ表示
- **主要操作のショートカット**：抽出=Ctrl/⌘+Return・音声生成=Ctrl/⌘+G・ファイル追加=Ctrl/⌘+O・txt保存=Ctrl/⌘+S・試聴=Ctrl/⌘+P・停止=Esc（ツールチップにも表記）
- **§3の置換バーを整理**：ルールの「登録/削除」を `ルール ▾` メニューにまとめ、常時グレーだった「復元」を「本文を全消去」と1ボタンに統合（全消去すると「復元」に切り替わる）
- **§4をすっきり**：使わない人も多い「プリセット/セリフ別話者」行を折りたたみ式に（右端の `プリセット/セリフ ▸` で開閉・状態は保存・セリフ別話者ON時は自動で開く）。区切りの余白も拡大
- 立ち絵パネルの自動開閉にヒステリシスとデバウンスを追加（境界幅でウィンドウをドラッグしたときのチラつきを解消）

## v1.11.0 — 2026-07-17（テーマを選べる・押しやすいボタン）

**見やすさと押しやすさ（機能・設定は不変）**
- **テーマが4種から選べるように**：`🍂 ライト` / `🌙 ダーク` / `☀️ くっきり` / `🌿 ずんだ`。「くっきり」は白地×濃い文字×強い枠線の**ハイコントラスト**（小さな文字が見づらい時に）。「ずんだ」はずんだもんの**若草色**テーマ。§2右上のプルダウンでいつでも切替、選択は次回起動にも引き継がれます（旧設定の `"dark": true` も自動でダークに引き継ぎ）。
- **主要ボタンを押しやすく**：`▶ テキスト抽出 実行` と `🔊 音声を生成` をひとまわり大きく。**生成ボタンは画面右下の端**（作業の終点＝マウスを流す先）へ移動し、誤クリックしやすかった「読み方辞書…」とは間隔を空けました。狭い窓でも生成ボタンが最後まで残ります。
- ライトテーマの文字色・アクセントのコントラストを微調整（見出しや説明文がくっきり）。
- **狭い窓でも大事なものから残る配置に**：テーマ選択・主要ボタンを優先確保し、あふれた時は状態文だけが縮むように。再生ボタンの補足（「カーソル行」等）はラベルからツールチップへ移し、`▶ 試聴`／`▶▶ 連続再生` と短く（最小幅860pxでも全ボタンが判読可能に）。

## v1.10.0 — 2026-07-17（キャラが生きて、導線が見える）

**見た目と分かりやすさの強化（機能・設定は不変）**
- **立ち絵が生きるようになりました**（公式の口パク差分を利用・任意）：`assets/立ち絵/` に `_closed`／`_open`／`_blink` の各フレームを置くと、待機中は**まばたき**、**試聴・連続再生の間は口パク**します。連続再生では**行の話者に合わせてキャラも切替**（@話者名タグにも追従）。フレームが無ければ従来どおり静止画のまま。
- **状態の吹き出し**：立ち絵の上に、いまの状態（待機中・生成中・完了…）を映す**吹き出し**を追加。キャラが教えてくれる見た目になり、進行が分かりやすくなりました（ライト/ダーク両テーマ対応）。
- **セクション見出しにアイコン**：`1. 📥 入力ファイル` → `2. ⚙️ 抽出オプション` → `3. ✏️ 抽出結果` → `4. 🔊 VOICEVOX へ` と、流れがひと目で追えるように。
- **状態表示が次の一歩を案内**：起動直後は「まずは 1. にPDFや画像を追加…」、抽出完了時は「3.で直して → 4.で音声に」と表示。はじめてでも迷いません。
- GUIスモークテストにアニメ安全性のテストを追加（フレーム欠如・未知話者IDでも落ちない）。

## v1.9.0 — 2026-07-15（キャラ立ち絵とテーマ刷新で美化）

**見た目の強化（機能・設定は不変）**
- **UI全体を「落ち葉」カラーで統一**：ライト／ダークの両方を `clam` ベースの**暖色パレット**で塗り直しました（ライトはmacOS標準のグレー外観をやめ、温かいクリーム地＋落ち葉オレンジのアクセントに）。ボタン・入力欄・チェック・ラジオ・進捗バー・スクロールバー・セクション枠まで配色を一括管理し、**主要ボタン（テキスト抽出／音声を生成）はアクセント色**で強調。クラスタ見出しはオレンジ、セクション見出しは濃い茶色で視線を整理しました。
- **キャラ立ち絵パネルを追加**：ウィンドウ右側に、選択中の話者に応じたキャラ（**ずんだもん／四国めたん**）の立ち絵を表示します。既定はマスコットのずんだもん、話者に「四国めたん」を選ぶと切り替わります。透過背景はライト／ダークのテーマに追従します。
- **窓が狭いときは自動で畳む**：横幅が足りないときは立ち絵パネルを隠し、本文・操作列の横幅を優先します（広げると自動で再表示）。
- **アプリアイコン**と、画面右下の**クレジット表記** `VOICEVOX / 立ち絵:坂本アヒル` を追加。
- **立ち絵は同梱していません（任意・ローカルのみ）**：画像は `assets/立ち絵/*.png` に置く方式で、リポジトリには含めません（公式素材の二次配布を避けるため `.gitignore` 済み）。**画像が無くても従来どおり動作**します（立ち絵枠が出ないだけ）。入手先・置き方・クレジットは [`assets/立ち絵/README.md`](assets/立ち絵/README.md) を参照。
  - 立ち絵素材：坂本アヒル 氏／管理：東北ずん子・ずんだもんプロジェクト（利用ガイドライン：https://zunko.jp/guideline.html ）。良識の範囲での利用・改変が許諾されています。

## v1.8.0 — 2026-07-15（画面の整理・簡易化）

**UIの整理（機能・設定は不変。見た目と配置だけを整えました）**
- **4つのセクションを意味のかたまりで再編**：`4. VOICEVOX へ` を **［接続］［声（話者と調整）］［出力（形式と保存）］［再生・生成］** の小見出し付きグループに分割。横一列に密集していた操作が役割ごとにまとまり、目的の操作を探しやすくなりました。
- **話速・音高・抑揚・音量を表の形で整列**：ラベルと入力欄を `grid` の列で縦に揃え、バラバラだった余白を数個の定数（`PAD_X`/`PAD_Y`/`GAPX`/`GAPY`）に統一しました。
- **整形オプションに「詳細設定」の折りたたみを追加**：よく使う5項目（画像前処理／空行を削除／英数字間の空白を保持／折り返しを連結／画面キャプチャのノイズを除去）は常時表示、上級者向けの3項目（改行で途切れた文を連結／括弧ルビ除去／全角英数→半角）は **「詳細設定 ▸」** で開閉できるようにし、既定は畳んで初見の情報量を減らしました（開閉状態は次回も保存）。
- **主要ボタンを強調**：`▶ テキスト抽出 実行` と `🔊 音声を生成` を primary スタイル（太字／ダーク時はアクセント色）で少し目立たせました。
- **紛らわしいラベル名を整理**（内部の設定キー・値は不変なので**旧 `settings.json` はそのまま読めます**）：
  - 抽出オプションの「出力形式」→ **「改行の扱い」**（文ごとに改行 / 元の改行を保持）。
  - VOICEVOX の「出力形式」→ **「音声形式」**（WAV/M4A/MP3）、「出力単位」→ **「まとめ方」**。
- **各チェック項目にツールチップ**（意味の短い説明）を追加。**セクション見出しを太字**にして区切りを明確化しました。

**直したところ / 内部**
- `_build_ui` を役割ごとの小さなビルダ（`_build_files_section`／`_build_options_section`／`_build_voicevox_section`／`_build_result_section`）に分割して見通しを改善（生成するウィジェット・pack順の意味・結線は従来と等価）。
- GUIスモークテスト（`tests/test_gui.py`）を追加：必須ウィジェット/変数の存在・busy切替・テーマ往復・設定往復・折りたたみ開閉を確認（Tk初期化不可の環境では自動 skip）。

## v1.7.0 — 2026-07-14（映像内ラベルの除去）

**新機能**
- **映像内ラベルの除去**：ニュース画像OCRに残りがちな**局ロゴ・番組名・日時・カテゴリ表示などの短い日本語ラベル**（例「MBSニュース」「国内」「TBS TBSテレビ」「国際」）を、行の**座標・文字サイズ**を使って除去します。これらは正しい日本語なのでテキストだけでは本文中の短語と区別できませんでした。本文（画面で最も支配的な段落）から外れた「①文字サイズが本文と違う ②画面最上部にある短い行 ③本文の折り返しではない孤立した短い行」のうち**複数条件を満たす行だけ**を落とします。**単一条件では落とさず**、句読点・鉤括弧を含む行／段落ブロックの一部／一定長以上の行は必ず残すため、**本文の取りこぼしは起きません**（迷ったら残す方針）。macOS（Apple Vision）でのみ有効。ON/OFFは「画面キャプチャのノイズを除去」（CLIは `--denoise`／`--no-denoise`）と共通で、OFFのときの出力は従来と完全に同じです。

**改善**
- 折り返し連結（`reflow_ocr_lines`）とラベル除去でブロック分割（縦に連続する同じ列の行のまとめ）を共有し、判定を一貫させました。

**直したところ**
- READMEの機能一覧で「画面キャプチャのノイズ除去」を**既定OFF**と誤記していたのを修正（v1.6.0で既定ONに変更済み。実際の挙動はONで、`--no-denoise`／チェックを外すとOFF）。

**原理的な限界（既知）**
- 「画面最上部・本文と別サイズ・孤立・ごく短い」行は、カテゴリ表示（国内/国際）と、稀にあるごく短い見出し（例「第一章」「速報」）を座標だけでは区別できません。この位置・形の行はラベルとして除かれることがあります（本機能の狙いは前者の除去なので許容範囲）。段落本文・句読点を含む行・一定長以上の見出しは常に残るため、記事本文の取りこぼしは起きません。気になる場合は「画面キャプチャのノイズを除去」をOFFにしてください。

## v1.6.0 — 2026-07-14（賢い区切り・使い勝手の改善）

**「画像を入れたら自動でキレイな記事テキストに」**：画像OCRでは、下の2つが自動で効くようにしました（設定不要）。
- **折り返しを座標で連結**：macOSのOCR（Apple Vision）が返す各行の位置・大きさ（外接矩形）を使い、折り返しで複数行に割れた1文を1文に戻します。見出し・箇条書き・別段落は「右端が短い」「行間が空く」ことから座標で判別し、右余白はブロック（縦に連続する同じ列）ごとに測るので、テキストだけの推測より誤連結を大幅に減らします。行末が鉤括弧「」や読点でも、折り返しなら正しく連結します。※行間が詰まった同じ幅の見出しが2本並ぶ等、座標でも区別できないケースは連結され得ます。
- **ノイズ除去を既定ON・強化**：局ロゴ・時刻・SNSハンドル・矢印などに加え、**埋め込みの英文ツイート等の外国語ブロックも除去**するようにし、既定でONにしました（`--no-denoise`／チェックを外せば従来どおり残せます）。句読点・ひらがなを含む日本語本文は必ず残します。英語主体の入力は丸ごと消えません。
- **折り返しを連結（テキスト向け・オプション）**：座標が無い入力（テキストファイル・テキスト層つきPDF）向けに、折り返しを推測で連結する簡易機能も用意。ただしテキストだけでは折り返しと別々の短い見出しを完全には区別できないため**既定OFF**（GUIの「折り返しを連結（1段組みの本文向け）」／CLIは`--smart-join`）。積極的に連結したいときは従来の「改行で途切れた文を連結（小説向け）」を使います。
- **本文の「全消去」「復元」**：抽出結果を一気に消せるボタンと、それを元に戻すボタンを追加。抽出・置換・手入力など本文が変わると「復元」は自動で無効化され、古い内容で上書きする事故を防ぎます（Ctrl/Cmd+Zでも戻せます）。
- **画像を画面のどこにでもドロップ**：ファイル一覧だけでなく、本文欄を含む画面のどこに画像・PDF・フォルダを落としても入力に追加できるようにしました。

**改善**
- 抽出オプションのチェックボックスを2段に整理し、詰め込みすぎを解消して見やすくしました。

## v1.5.0 — 2026-07-14（画面キャプチャのノイズ除去）

**新機能**
- **画面キャプチャのノイズを除去**：ニュース番組やSNS埋め込みを含む「画面キャプチャ」のOCRから、読み上げ対象でない“映像内オーバーレイ文字”（局ロゴ・時刻・日付・SNSハンドル・矢印・単独の数値など）を自動で取り除きます。判定は1行単位で保守的に行い（迷ったら残す）、句読点・鉤括弧・ひらがなを含む本文は必ず残します。既定はOFF。GUIは整形オプションの「画面キャプチャのノイズを除去」、CLIは `--denoise` で有効化できます。オフにしたときの出力は従来と完全に同じです。

## v1.4.1 — 2026-07-13（配布向けの地固め）

派手な新機能はなし。人に渡しても最初のところで詰まらない・固まらない、を目的にした安定化です。

**直したところ**
- 数値の欄（OCR解像度・話速・音高・抑揚・音量・無音）を空にしたまま実行すると、画面全体が固まって操作できなくなることがあった問題を修正。
- 前回サブモニタで閉じたあと、そのモニタを外して起動すると窓が画面の外に出て「起動しない」ように見えることがあった問題を修正（画面内に戻します）。
- 章のファイル名に日本語や空白を含む EPUB で、章がまるごと読み飛ばされることがあった問題を修正。全章読めなかったときはエラーで気づけるようにしました。
- 「連続再生（カーソル行から）」で、末尾の空行にカーソルがあると先頭から再生し直してしまう挙動を修正。
- 連続再生の最中にテキスト抽出などを始められてしまい、再生中の本文が入れ替わる不整合を防止。
- M4A/MP3 変換に失敗したとき、日本語のフォルダ名が原因でエラー表示自体が化けて落ちることがあったのを修正。

**配布まわり**
- まっさらな Windows（Python 未導入）で `setup.bat` を動かしたとき、Microsoft Store のダミー python に当たって不親切なメッセージで止まっていたのを改善。Python の入れ方（python.org / "Add python.exe to PATH"）を案内します。
- 依存ライブラリを `requirements.txt` に一本化し、バージョンに上限を付けました（将来ライブラリ側の破壊的更新で新規インストールだけ壊れるのを防ぐため）。
- アプリのバージョンをタイトルバー・`cli.py --version` に表示。不具合報告のときに版が分かります。

**後片付け・その他**
- OCR に使った一時ファイル（元文書の画像コピー・抽出テキスト）を、処理後に必ず削除するようにしました（%TEMP% に残らない）。
- CLI: `--srt` を `--combine` なしで指定したときに警告を出すように。`@話者名:` タグを GUI と同じく解釈するように（タグは読み上げず、その行だけ話者を切り替え）。

## v1.4.0 — 2026-07-12

道具箱スタイルへの刷新と、多数の機能追加（連続再生・しおり・行別話者・プリセット・字幕出力・CLI ほか）。
